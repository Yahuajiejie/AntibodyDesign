# ══════════════════════════════════════════════════════
# FLAb 抗体亲和力预测项目 — 完整依赖列表
# 安装：pip install -r requirements.txt
# ══════════════════════════════════════════════════════

# ── 深度学习核心 ────────────────────────────────────────
# PyTorch：模型训练、GPU 加速
# 建议从官网选对应 CUDA 版本安装：https://pytorch.org/get-started/locally/
torch>=2.1.0

# HuggingFace Transformers：加载 ESM2 预训练模型
transformers>=4.36.0

# ── 科学计算 ─────────────────────────────────────────
numpy>=1.26.0
scipy>=1.11.0        # Spearman 相关系数计算
pandas>=2.0.0        # 数据集读取与操作
openpyxl>=3.1.0      # 读取 ANDD.xlsx 等 Excel 数据源

# ── 机器学习工具 ─────────────────────────────────────
scikit-learn>=1.3.0  # train_test_split 数据集划分

# ── 可视化 ───────────────────────────────────────────
matplotlib>=3.8.0    # 所有统计图和原理图

# ── 其他工具 ─────────────────────────────────────────
# tqdm：进度条（可选，embedding 提取时显示进度）
tqdm>=4.65.0
