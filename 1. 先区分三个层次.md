可以，先给结论：**`heavy_chain + light_chain` 可以表达“经典双链抗体”的核心序列，但不够表达所有天然抗体、人工抗体和基因工程抗体。**
如果你的 schema 只是为了做常规抗体亲和力排序，`heavy_chain / light_chain / antibody_type` 这一套可以先跑；但如果你希望兼容 VHH、scFv、Fab、Fc-fusion、双特异/多特异抗体、ADC、CAR 里的抗体片段，就需要更通用的表示方式。

## 1. 先区分三个层次

你说的“天然抗体、人工抗体、基因工程抗体”其实容易混在一起。更清楚的分类方式是：

**天然来源抗体**：生物体内天然产生的抗体，比如人、小鼠、兔、羊驼/骆驼的抗体。

**抗体片段/重组抗体格式**：把天然抗体的某些结构域拆出来或重组，比如 Fab、Fv、scFv、VHH。

**工程化治疗抗体格式**：在抗体或抗体片段基础上进一步工程改造，比如人源化抗体、双特异抗体、ADC、Fc-fusion、CAR 的 scFv 识别域等。

经典 IgG 抗体由两条相同 heavy chain 和两条相同 light chain 构成；每条链都有 N 端可变区和 C 端恒定区，其中可变区参与抗原识别，heavy chain 的类型决定抗体同种型和效应功能。([国家生物技术信息中心](https://www.ncbi.nlm.nih.gov/books/NBK27144/?utm_source=chatgpt.com))

------

## 2. 天然抗体有哪些？序列构成是什么？

### 2.1 经典哺乳动物抗体：IgG / IgA / IgM / IgE / IgD

这是最常见、也是大多数抗体数据集默认处理的对象。

它们的基本结构是：

```text
2 × Heavy chain + 2 × Light chain
```

其中：

```text
Heavy chain = VH + CH1 + hinge + CH2 + CH3   # 以 IgG 为例
Light chain = VL + CL
```

如果只看抗原结合区域，通常关注：

```text
Fv = VH + VL
Fab = VH + CH1 + VL + CL
```

所以对经典 IgG/Fab/Fv 来说，`heavy_chain + light_chain` 是合理的。
但这里要注意：如果 `heavy_chain` 存的是**完整重链**，里面包含 VH、CH1、hinge、CH2、CH3；如果存的是**可变区重链 VH**，那它只表示结合位点的一半。你的 schema 最好明确字段语义。

建议不要只叫：

```text
heavy_chain
light_chain
```

而是至少在文档里说明：

```text
heavy_chain: 优先存 VH；若数据提供 full-length heavy chain，则需要额外标记 chain_region。
light_chain: 优先存 VL；若数据提供 full-length light chain，则需要额外标记 chain_region。
```

否则后面模型会把 VH 和 full-length IgG heavy chain 混在一起。

------

### 2.2 骆驼科 heavy-chain-only antibody / VHH / Nanobody

羊驼、骆驼等骆驼科动物除了常规抗体，还天然存在 heavy-chain-only antibodies。这类抗体**没有 light chain**，并且 heavy chain 中通常缺少 CH1 domain；其单独负责抗原识别的可变结构域称为 VHH，也就是常说的 nanobody。([公共医学中心](https://pmc.ncbi.nlm.nih.gov/articles/PMC2039825/?utm_source=chatgpt.com))

序列构成大致是：

```text
HCAb = VHH + hinge + CH2 + CH3
VHH  = 单个重链可变结构域
```

如果你的数据是一条 nanobody 序列，通常就是：

```text
heavy_chain = VHH
light_chain = None
antibody_type = "VHH"
```

所以 `heavy_chain + light_chain` 可以勉强兼容 VHH，但语义上会有点别扭：VHH 不是普通 VH，它来自 heavy-chain-only antibody，表面亲水性、CDR3 特征、稳定性都不完全一样。最好单独有：

```text
antibody_type = "VHH"
chain_format = "single_domain"
```

------

### 2.3 软骨鱼 IgNAR / VNAR

鲨鱼等软骨鱼还有另一类 heavy-chain-only-like 抗体，常见抗原结合域叫 VNAR。这个在治疗抗体数据里没 VHH 常见，但从“抗体格式兼容性”角度要知道它存在。

序列上可以粗略理解为：

```text
VNAR = 单结构域抗原结合片段
```

如果数据集将来包含 VNAR，`heavy_chain + light_chain` 也不自然，因为它既不是普通 heavy-light pair，也不是 VHH。更通用的字段应该允许：

```text
chains = [
  {chain_id: "A", chain_role: "VNAR", sequence: "..."}
]
```

------

## 3. 人工/重组抗体片段有哪些？序列构成是什么？

### 3.1 Fab

Fab 是抗体的抗原结合片段，由一条完整 light chain 和一条 heavy chain 的 VH+CH1 部分组成。也就是：([赛诺生物](https://www.sinobiological.com/resource/antibody-technical/fab-antibody-production?utm_source=chatgpt.com))

```text
Fab = VH + CH1 + VL + CL
```

如果数据只给可变区，可以表示成：

```text
heavy_chain = VH
light_chain = VL
antibody_type = "Fv" 或 "Fab_variable_only"
```

如果数据给完整 Fab 序列，那么：

```text
heavy_chain = VH + CH1
light_chain = VL + CL
antibody_type = "Fab"
```

这里 `heavy_chain + light_chain` 是够用的，但要知道它们不是 full IgG。

------

### 3.2 Fv

Fv 是最小的经典双链抗原结合单位：

```text
Fv = VH + VL
```

它没有 CH1、CL、Fc。
这个和你们目前亲和力预测任务最相关，因为很多序列数据集只提供 VH/VL。

```text
heavy_chain = VH
light_chain = VL
antibody_type = "Fv"
```

这套表示完全够用。

------

### 3.3 scFv

scFv 是 single-chain variable fragment，它把 VH 和 VL 用一段柔性 linker 接成一条多肽链。常见构型是：([Addgene Blog](https://blog.addgene.org/antibodies-101-single-chain-fragment-variables-scfvs?utm_source=chatgpt.com))

```text
scFv = VH - linker - VL
```

或者：

```text
scFv = VL - linker - VH
```

linker 常常是 Gly/Ser 富集的柔性肽段，比如 `(G4S)n` 一类。

这时 `heavy_chain + light_chain` 就不太够了。因为 scFv 的真实输入序列是一条连续多肽：

```text
single_chain_sequence = VH + linker + VL
```

如果你硬拆成：

```text
heavy_chain = VH
light_chain = VL
```

会丢掉：

```text
linker 序列
VH/VL 顺序
是否为单链表达
```

而这些会影响表达、折叠和工程性质。

所以 schema 里如果要支持 scFv，最好加：

```text
single_chain_sequence: str | None
chain_order: "VH-linker-VL" | "VL-linker-VH" | None
linker_sequence: str | None
```

------

### 3.4 VHH / Nanobody

VHH 既可以视为天然 heavy-chain-only antibody 的可变域，也可以作为工程化单域抗体格式使用。它的构成是：

```text
VHH = single variable domain
```

通常没有 light chain，也不需要 VL 配对。VHH/scFv 都是现代靶向治疗中常见的抗体片段格式。([公共医学中心](https://pmc.ncbi.nlm.nih.gov/articles/PMC8642758/?utm_source=chatgpt.com))

表示方式：

```text
heavy_chain = VHH
light_chain = None
antibody_type = "VHH"
```

但更干净的表示是：

```text
chains = [
  {chain_role: "VHH", sequence: "..."}
]
```

------

### 3.5 Fc-fusion / scFv-Fc / VHH-Fc

这些是把抗体片段接到 Fc 上，以增加半衰期、二聚化能力或效应功能。

常见构成：

```text
scFv-Fc = scFv + Fc
VHH-Fc  = VHH + Fc
Fab-Fc  = Fab-like binding arm + Fc
```

如果只存 `heavy_chain + light_chain`，会丢失 Fc、连接肽、融合顺序等信息。

如果你的任务只预测结合亲和力，可能可以先只取 binding domain；但如果未来要预测表达、稳定性、半衰期、聚集、功能活性，Fc 和 linker 就不能丢。

------

## 4. 基因工程抗体有哪些？序列构成是什么？

### 4.1 嵌合抗体、人源化抗体、全人抗体

这些更多是“来源/工程程度”的分类，而不一定是结构格式分类。

它们通常仍然是经典 IgG：

```text
2 × heavy chain + 2 × light chain
```

区别在于序列来源：

```text
嵌合抗体：鼠源可变区 + 人源恒定区
人源化抗体：主要为人源框架区 + 移植 CDR
全人抗体：可变区和恒定区整体更接近人源序列
```

所以 `heavy_chain + light_chain` 对它们通常是够的。
但如果你关心工程属性，最好加：

```text
species_origin
humanization_status
isotype
```

例如：

```text
species_origin = "mouse" | "human" | "camelid" | "rabbit" | "mixed" | "unknown"
engineering_type = "chimeric" | "humanized" | "fully_human" | "synthetic" | "unknown"
```

------

### 4.2 双特异抗体 Bispecific antibodies

双特异抗体可以同时识别两个不同抗原或两个不同表位。它们格式非常多，FDA 也有专门的双特异抗体开发指导文件，说明这已经是独立且复杂的一类治疗分子。([U.S. Food and Drug Administration](https://www.fda.gov/regulatory-information/search-fda-guidance-documents/bispecific-antibody-development-programs-guidance-industry?utm_source=chatgpt.com))

常见大类是：

```text
IgG-like bispecific
non-IgG-like bispecific
```

文献中也常这样分类。([公共医学中心](https://pmc.ncbi.nlm.nih.gov/articles/PMC8131538/?utm_source=chatgpt.com))

#### IgG-like bispecific

可能构成是：

```text
Heavy chain A + Light chain A
Heavy chain B + Light chain B
```

也就是不是一条 heavy + 一条 light，而是：

```text
heavy_chain_1
light_chain_1
heavy_chain_2
light_chain_2
```

并且还要处理 heavy-light 正确配对问题。双特异 IgG 的设计和生产经常涉及链错配控制，这是这类分子的核心工程问题之一。([公共医学中心](https://pmc.ncbi.nlm.nih.gov/articles/PMC5297537/?utm_source=chatgpt.com))

所以 `heavy_chain + light_chain` 不够。

#### non-IgG-like bispecific

比如：

```text
BiTE = scFv(antigen A) - linker - scFv(antigen B)
```

这其实是一条连续多肽：

```text
VH_A-linker-VL_A-linker-VH_B-linker-VL_B
```

这时更不适合用 `heavy_chain + light_chain` 表示。

------

### 4.3 多特异抗体 Trispecific / multispecific antibodies

三特异或多特异抗体通常会在 IgG、Fab、scFv、VHH 等 building blocks 基础上继续拼接。近年的综述也会把 Fab、scFv、Fc、VHH 等称为多特异抗体工程中的常用构件。([OUP Academic](https://academic.oup.com/abt/article/7/1/37/7463325?utm_source=chatgpt.com))

比如：

```text
IgG + scFv
IgG + VHH
scFv + scFv + Fc
VHH + VHH + VHH
```

这类结构必须用更通用的“多链/多片段”表示。

------

### 4.4 ADC：抗体偶联药物

ADC 的蛋白部分通常还是 monoclonal antibody，常见是 IgG；但完整分子还包括 linker 和 payload：

```text
Antibody + chemical linker + cytotoxic payload
```

如果你只做抗原结合亲和力预测，`heavy_chain + light_chain` 可以表示 ADC 的抗体部分。
但如果你研究 ADC 整体性质，比如 DAR、药物释放、毒性、内吞，单靠 heavy/light chain 就完全不够。ADC 综述通常会把靶点选择、linker、payload 和偶联化学都作为关键设计因素。([Nature](https://www.nature.com/subjects/antibody-therapy/nrd?utm_source=chatgpt.com))

------

### 4.5 CAR-T / CAR-NK 中的抗体识别域

CAR 不是抗体本身，但它常使用 scFv 或 VHH 作为抗原识别域。

常见构成：

```text
scFv or VHH
+ hinge/spacer
+ transmembrane domain
+ intracellular signaling domains
```

如果你只关心 CAR 的抗原识别，应该抽出 scFv/VHH；如果要表示完整 CAR，`heavy_chain + light_chain` 肯定不够。

------

## 5. `heavy_chain + light_chain` 到底够不够？

我建议你这样判断。

### 够用的情况

如果你的数据主要是：

```text
IgG variable region
Fv
Fab variable region
常规 monoclonal antibody
普通 VH/VL paired antibody
```

那么：

```text
heavy_chain
light_chain
antibody_type
```

基本够用。

尤其对你们现在的亲和力排序任务，如果数据集大多只提供 VH/VL 或 VHH，这个 schema 可以作为 v0.1。

------

### 勉强够用但需要额外字段的情况

如果包含：

```text
VHH / nanobody
scFv
Fab full fragment
humanized/chimeric/full-human antibody
```

那 `heavy_chain + light_chain` 还可以用，但必须加字段解释它们的语义：

```text
antibody_type
chain_format
chain_region
linker_sequence
single_chain_sequence
species_origin
engineering_type
```

否则后面你不知道 `heavy_chain` 到底是 VH、VHH、VH+CH1，还是 full-length heavy chain。

------

### 不够用的情况

如果包含：

```text
bispecific antibody
trispecific antibody
multispecific antibody
scFv-Fc
VHH-Fc
IgG-scFv
ADC 完整分子
CAR 完整构型
```

那 `heavy_chain + light_chain` 不够。
因为它们可能有多条 heavy chain、多条 light chain、多段 scFv/VHH、多段 linker、Fc、payload 或非抗体结构域。

这时应该改成更通用的表示。

------

## 6. 我建议你的 schema 采用“两层表示”

你现在的 schema 可以保留，因为它对主流数据集友好：

```python
heavy_chain: str
light_chain: str | None
antibody_type: "Fv" | "scFv" | "VHH" | "unknown"
```

但建议再加一个可选的通用字段：

```python
chain_schema_version: str
chain_format: str
single_chain_sequence: str | None
chain_components: list[dict] | None
```

例如普通 Fv：

```json
{
  "antibody_type": "Fv",
  "heavy_chain": "QVQL...",
  "light_chain": "DIQL...",
  "chain_format": "paired_VH_VL",
  "chain_components": [
    {"name": "VH", "role": "heavy_variable", "sequence": "QVQL..."},
    {"name": "VL", "role": "light_variable", "sequence": "DIQL..."}
  ]
}
```

VHH：

```json
{
  "antibody_type": "VHH",
  "heavy_chain": "QVQL...",
  "light_chain": null,
  "chain_format": "single_domain",
  "chain_components": [
    {"name": "VHH", "role": "single_domain_binder", "sequence": "QVQL..."}
  ]
}
```

scFv：

```json
{
  "antibody_type": "scFv",
  "heavy_chain": "QVQL...",
  "light_chain": "DIQL...",
  "single_chain_sequence": "QVQL...GGGGSGGGGSGGGGS...DIQL...",
  "chain_format": "single_chain_VH_linker_VL",
  "chain_components": [
    {"name": "VH", "role": "heavy_variable", "sequence": "QVQL..."},
    {"name": "linker", "role": "linker", "sequence": "GGGGSGGGGSGGGGS"},
    {"name": "VL", "role": "light_variable", "sequence": "DIQL..."}
  ]
}
```

双特异 IgG-like：

```json
{
  "antibody_type": "bispecific_IgG_like",
  "heavy_chain": null,
  "light_chain": null,
  "chain_format": "multi_chain",
  "chain_components": [
    {"name": "HC_A", "role": "heavy_chain_arm_1", "sequence": "..."},
    {"name": "LC_A", "role": "light_chain_arm_1", "sequence": "..."},
    {"name": "HC_B", "role": "heavy_chain_arm_2", "sequence": "..."},
    {"name": "LC_B", "role": "light_chain_arm_2", "sequence": "..."}
  ]
}
```

这样你既能兼容简单数据，也不把未来堵死。

------

## 7. 对你当前项目的实际建议

你现在不要一上来把 schema 写得过度复杂。对于当前亲和力排序任务，我建议：

```python
heavy_chain: str | None
light_chain: str | None
single_chain_sequence: str | None

antibody_type: (
    "Fv"
    | "Fab"
    | "scFv"
    | "VHH"
    | "IgG"
    | "bispecific"
    | "unknown"
)

chain_format: (
    "paired_VH_VL"
    | "single_domain"
    | "single_chain"
    | "full_length_IgG"
    | "multi_chain"
    | "unknown"
)

chain_region: (
    "variable_only"
    | "fab"
    | "full_length"
    | "unknown"
)
```

然后规定：

```text
如果 antibody_type == "Fv":
    heavy_chain = VH
    light_chain = VL

如果 antibody_type == "VHH":
    heavy_chain = VHH
    light_chain = None

如果 antibody_type == "scFv":
    single_chain_sequence 必须优先保留
    heavy_chain / light_chain 可由解析得到
    linker_sequence 如果能解析则保留

如果 antibody_type == "bispecific":
    v0.1 暂不进入训练，除非能拆成单一 binding arm
```

这会比单纯 `heavy_chain + light_chain` 稳很多。

------

## 8. 一句话总结

**`heavy_chain + light_chain` 是经典抗体的表示法，不是所有抗体工程格式的通用表示法。**

对你们当前项目，建议保留它作为主字段，但加上：

```text
single_chain_sequence
chain_format
chain_region
```

如果以后要兼容双特异、多特异、Fc-fusion、CAR、ADC，就再升级为 `chain_components` 这种多片段表示。